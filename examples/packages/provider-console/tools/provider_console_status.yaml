name: provider_console_status
description: Show a compact provider console snapshot.
command: "{{ .tool_dir }}/provider_console_status.sh"
schema:
  type: object
  properties: {}
ui:
  default_collapsed: true
  collapsed_summary: Provider console refreshed.
